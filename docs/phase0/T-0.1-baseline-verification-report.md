# T-0.1 Baseline Build / Watch / Launch 验收报告

> **任务**：`docs/plans/002-development-task-breakdown.md` § T-0.1
> **审核**：Hermes 主代理
> **日期**：2026-06-19
> **结论**：**PASS（WSL/Linux selfhost）+ Windows native install 列为已知问题，不阻塞 Phase 1**

---

## 1. 验证范围

| 环境 | install | watch / compile | launch |
|---|---|---|---|
| WSL Linux selfhost | ✅ 通过 | ✅ 通过 | ✅ 通过（需 `--disable-gpu`） |
| Windows native | ❌ 失败（已知问题） | — | — |

## 2. 证据

### 2.1 WSL/Linux selfhost — npm install

- 日志：`docs/phase0/logs/npm-install.log`
- 结果：`added 1569 packages, ... 28 vulnerabilities ... === npm install exit status: 0 ===`
- 时间：2026-06-16 10:47

### 2.2 WSL/Linux selfhost — watch / compile

- 日志：`docs/phase0/logs/npm-watch.log`
- 结果（关键行）：
  ```
  [watch-client          ] [10:40:08] Finished watch-client-noEmit /mnt/d/git/vsword/code-oss/src/tsconfig.json with 0 errors.
  [watch-client          ] [10:40:08] Finished compilation with 0 errors after 79927 ms
  [watch-client          ] [10:40:20] Finished compilation extension-point-names with 0 errors after 91281 ms
  [watch-copilot         ] [watch:typecheck] 10:38:51 AM - Found 0 errors. Watching for file changes.
  ```
- 时间：2026-06-17 10:38–10:40
- 备注：`watch-copilot` 是 Code OSS 自带的 typecheck watcher，0 errors 不代表内置 Copilot 入口，只代表该子项目类型检查通过。Copilot 默认入口的移除属于 T-1.3 的工作范围，不影响 T-0.1。

### 2.3 WSL/Linux selfhost — launch

- 日志：`docs/phase0/logs/code-launch-disable-gpu.log`
- 结果：Extension host 多次进入 responsive，VS Code 启动成功
- 时间：2026-06-17 16:35
- **已知项**：直接 `./scripts/code.sh` 在当前 WSL+RTX2080Ti 环境下 GPU 沙盒崩溃；加 `--disable-gpu` 即正常。这是 WSL/Linux selfhost 普遍现象，不属于 VSWord 引入的问题。

### 2.4 Windows native — npm install 失败（已知问题）

- 日志：`docs/phase0/logs/01-npm-install-windows.log`
- 时间：2026-06-18 19:32
- 失败位置：`native-keymap@3.3.9` 的 `node-gyp rebuild`
- 关键尾部：
  ```
  npm error gyp ERR! cwd D:\GIT\VSWord\code-oss\node_modules\native-keymap
  npm error gyp ERR! node -v v24.14.1
  npm error gyp ERR! node-gyp -v v12.2.0
  npm error gyp ERR! not ok
  EXIT=1
  ```
- 环境快照：`docs/phase0/logs/00-windows-baseline-snapshot-20260618-1906.log`
  - Node：v24.14.1（Windows 全局，`.nvmrc` 要求 24.15.0；小版本差不足以解释失败）
  - VS Build Tools：`C:\Program Files\Microsoft Visual Studio\2022\Community` 已安装
  - Python：3.11.15 / 3.13.14 都在 PATH

## 3. 已知问题登记

| ID | 问题 | 影响 | 处理 |
|---|---|---|---|
| KI-T01-001 | Windows native `npm install` 在 `native-keymap` node-gyp rebuild 阶段失败 | 不能在 Windows 原生环境编译 | 不阻塞 MVP；Phase 1–5 全程使用 WSL/Linux selfhost；待进入 Windows 打包阶段（Pre-Beta）再修复（候选方案：补齐 Desktop C++ workload / Windows SDK / 切到 Node 24.15.0 / 用 fnm 隔离） |
| KI-T01-002 | WSL 直接启动 GPU 沙盒崩溃，需 `--disable-gpu` | 启动方式调整 | 工作流约定：Phase 1–5 默认 `./scripts/code.sh --disable-gpu`，写入 fullstack-developer skill 的 launch reference |
| KI-T01-003 | `extensions/terminal-suggest/.../symlink-test/*.sh` 被删除（git 显示 D） | 与 VSWord 无关，是 Windows checkout 把 symlink 还原成普通文件的副作用 | 不修改；维持 working tree 不动；后续若运行 terminal-suggest 测试再处理 |

## 4. Gate A 复核

- [x] Code OSS 源码就位
- [x] tag / commit 记录在案（1.124.2 / 6928394）
- [x] 未改源的 baseline install / watch / launch 在 WSL 通过
- [x] 全部日志归档于 `docs/phase0/logs/`
- [x] 失败清单已被审核人（主代理）接受为已知问题

## 5. 结论与下一步

- T-0.1 **PASS**（WSL 端）。
- Windows native install 列为已知问题（KI-T01-001），不阻塞下游任务。
- 解锁 T-1.1（VSWord contrib 模块骨架）。
