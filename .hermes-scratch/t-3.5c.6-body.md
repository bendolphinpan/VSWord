本子任务范围（T-3.5c.6 · Gate G 收官 + 模块 c 汇总）
━━━━━━━━━━━━━━━━━━━━━━

依据 PRD docs/plans/003c-syntax-completion.md（F-22..F-24 · §8 T-3.5c.6）：

F-22 · 全体特性接入 roundtrip-tracker.blockId，`build-result.json` 新增 fixture 覆盖所有 P0/P1 特性（Must · P0/P1）
F-23 · Definition list (`term\n:  def`) 解析 + 只读渲染（Could · P2 · 可推迟）
F-24 · slash-menu 增加 `/emoji` `/footnote` `/frontmatter` 快速插入入口（Could · P2 · 视 slash-menu 现状）

依赖：T-3.5c.1..5 全部 green（已完成 · 5 个 commit `3bd20467/353b4477/abc0a5e4/a1a963d2/05247156/70e4f7e7`）

━━━━━━━━━━━━━━━━━━━━━━
DoD
━━━━━━━━━━━━━━━━━━━━━━
1. `build-result.json` 汇总 syntax-completion fixture 组：
   - emoji（T-3.5c.1）· footnote（T-3.5c.2）· frontmatter YAML/TOML/JSON（T-3.5c.3）
   - sub/sup（T-3.5c.4）· code-block info meta（T-3.5c.5a）· setext（T-3.5c.5b）
   - 每类 fixture parserRoundTripBytes 与源码 byte-for-byte 匹配
2. Gate G CI 定义 · 汇总跑一遍全绿（tsc 0 · vsword 全套单测 ≥ 531 且无回归 · Gate D/E/F 全绿）
3. F-23 Definition list：如 remark-gfm 未自带 pandoc-style definition list，尝试挂 remark-definition-list；撞壁则降级为「已识别但作为普通段落展示」并在 kanban comment 说明
4. F-24 slash-menu：查 T-3.3.3 slash 是否已就位。已就位 → 挂 /emoji /footnote /frontmatter 三个 command；未就位 → 跳过，comment 记录 "slash-menu 未就位，跳过 F-24"
5. commit 本地不 push · title 'chore(vsword/milkdown): T-3.5c.6 Gate G 收官 + 模块 c 汇总'
6. **模块 c 收官 · Gate 全绿 → 直接 `git push origin dev` 一次性推 6 个 commit**（3bd20467/353b4477/abc0a5e4/a1a963d2/05247156/70e4f7e7/本次收官）· 不需要问用户
7. kanban comment 报最终状态 + push 结果（remote sha）+ blocked({kind:'needs_input', reason:'review-required: T-3.5c.6 收官 · 模块 c 完成'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━
- iteration 60 · 估 15-25
- 临近 45 优先 commit + push
- 优先级：**F-22 fixture 汇总 + Gate G CI + push > F-24 slash-menu > F-23 definition list**
- F-23/F-24 都是 Could P2，都可以推 phase 4 或干脆 wontfix，写清楚就行
- 实施细节自决，无方向性问题不问

指导：好看放一放 · 中文 · 无方向性问题自决 · 收官必须 push
