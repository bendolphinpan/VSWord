本子任务范围（PM · 模块 b 剩余部分 PRD 撰写）
━━━━━━━━━━━━━━━━━━━━━━

依据 003 master plan §4a 决策 F-1=B 全套（Mermaid + Flowchart.js + js-sequence-diagrams）：
- Mermaid 已完成（`docs/plans/003b-mermaid.md` + commit `a903d4d7/d094751f/49986162/c9baf55c/08277a0c`）
- **本子任务**：撰写 `docs/plans/003b-flowchart-jsseq.md` 覆盖 Flowchart.js + js-sequence-diagrams 两图库

━━━━━━━━━━━━━━━━━━━━━━
必要模仿参考
━━━━━━━━━━━━━━━━━━━━━━

1. **`docs/plans/003b-mermaid.md`**（同模块首个 PRD · 224 行）
   - §1 背景 / §2 目标 / §3 非目标 / §4 用户故事 / §5 特性清单 + 图类型分级
   - §6 验收标准（10 条 Given/When/Then）/ §7 技术约束 / §8 子任务拆解
   - §9 风险 / §10 默认假设 / §11 待用户拍板问题
   - **本次 PRD 结构与之对齐**，方便 dev 交叉阅读
2. **既有 NodeView 参照**（架构层已明确）：
   - `mermaid-view.template.js`（模块 b 已产）
   - `math-view.template.js`（T-3.9 · click-to-edit + textarea + Esc/Ctrl+Enter commit + live preview）
   - `code-block-chrome.template.js`（Prism 高亮 + code_block[lang=xxx] 通道）
   - `roundtrip-tracker.template.js` + `blockIdAllocator.ts`（走 code_block 通道无需破 Round-trip 契约）

━━━━━━━━━━━━━━━━━━━━━━
PRD 内容 DoD
━━━━━━━━━━━━━━━━━━━━━━

1. **两图库定位**（§1 背景）
   - Flowchart.js: `flowchart.js` npm · 最新稳定版号 · Typora 生态使用 `flow` fence
   - js-sequence-diagrams: `@rokt33r/js-sequence-diagrams` 或 `js-sequence-diagrams` npm · Typora 用 `sequence` fence
   - 与 Mermaid 关系：不重叠不冲突（Mermaid 走 `mermaid` fence · 这两库走独立 fence · code_block[lang=flow|sequence]）

2. **G 目标 + NG 非目标**（§2/§3）
   - G1 inline WYSIWYG（fence 直接渲染 SVG）· G2 click-to-edit + live preview
   - G3 Round-trip byte-for-byte · G4 主题联动 · G5 语法错误可见 · G6 首屏零负担（懒加载）
   - NG: 不做拖拽建图 · 不做独立文件类型 · 不做 mermaid v10 兼容降级 · 不做 CSS 深度定制（Phase 3 policy "好看放一放"）

3. **特性清单 F-01..F-15**（§5 · mirror mermaid PRD 结构）
   - F-01~F-06 复用 mermaid pattern（inline / click-edit / commit / 错误 banner / 主题 / 懒加载）
   - F-07~F-09 命令（copy source / export svg / export png）
   - F-10 空块占位 · F-11 只读禁编 · F-12 slash /flow /sequence 入口
   - F-13 Round-trip code_block 通道 · F-14 context key · F-15 host↔webview 协议
   - **Flowchart.js 特有**：直接 DOM 渲染（不返回 SVG 字符串），需要 host div · Raphaël 依赖注意 CSP
   - **js-sequence-diagrams 特有**：需要 Snap.svg / Raphaël + underscore + WebFont · 依赖树重（评估懒加载可行性）

4. **图类型/语法覆盖**（§5.2）
   - Flowchart.js 语法节点类型：`start` / `end` / `op` / `cond` / `inputoutput` / `subroutine` · 边样式（yes/no · 方向）
   - js-sequence-diagrams 语法：actor / message（-> · -->  · ->>  · -->>）· note over/left of/right of

5. **10 条 AC**（§6 · mirror mermaid PRD）

6. **技术约束**（§7）
   - CSP：两库如需 `eval` / `new Function` / `document.write` **必须走 spike 验证**（T-3.5b.2a spike）
   - Bundle 尺寸预估 + 懒加载策略（每个库独立 chunk · 首屏 0KB）
   - Raphaël / Snap.svg 依赖版本冲突风险

7. **子任务拆解**（§8）— **强制 3~5 个 T-号**：
   - T-3.5b-flow.1 · Flowchart.js spike（CSP + bundle）
   - T-3.5b-flow.2 · Flowchart.js NodeView + 懒加载 + fixture
   - T-3.5b-seq.1 · js-sequence-diagrams spike（依赖树 + CSP）
   - T-3.5b-seq.2 · js-sequence-diagrams NodeView + 懒加载 + fixture
   - T-3.5b-flowseq.3 · 两库 Gate F fixture 汇总 + 主题联动 + 收官

8. **风险 §9** · **默认假设 §10** · **待用户拍板方向性问题 §11**（≤5 题 a/b/c）

━━━━━━━━━━━━━━━━━━━━━━
硬约束
━━━━━━━━━━━━━━━━━━━━━━

- **只写 PRD，不做实施**
- **不用装 npm 包** — 版本号查 `npm view flowchart.js` / `npm view js-sequence-diagrams`（terminal 允许）
- **允许在 PRD 里预设默认假设**（mirror mermaid PRD §10）· 用户可推翻
- **必须尽量少方向性分歧**（把大部分决策写成默认假设 D-1..D-n，只留 ≤5 题真的分歧）
- **commit**：`docs(plans): 003b-flowchart-jsseq PRD v1（模块 b 剩余部分）` · 本地不 push
- 完成后 blocked({kind:'needs_input', reason:'review-required: 003b-flowchart-jsseq PRD v1 完成'}) 让 coordinator + 用户 review 默认假设 + 5 题

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━

- iteration 60 · 估 15-25
- 临近 45 优先 commit
- 优先级：**§1~§8（PRD 主体）> §9/10/11 > 完美排版**
- 无方向性问题自决 · 遇到必须问的写进 §11 就行

指导：好看放一放 · 中文 · 复制粘贴 003b-mermaid.md 结构再改内容最快
