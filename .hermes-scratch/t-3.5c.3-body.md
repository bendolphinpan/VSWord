本子任务范围（T-3.5c.3 · Frontmatter）
━━━━━━━━━━━━━━━━━━━━━━

依据 PRD docs/plans/003c-syntax-completion.md（F-10..F-16 · §8 T-3.5c.3）：

F-10 · 识别文件顶部 YAML（`---\n...\n---`）为独立 NodeView（Must · P0）
F-11 · 默认渲染折叠摘要行「📄 title · N 个字段」，点击展开 textarea（Must · P0）
F-12 · textarea 编辑 · Esc 取消 · Ctrl+Enter/失焦 commit（复用 math-view 交互）（Must · P0）
F-13 · YAML 语法错误 banner（js-yaml parse 失败时不阻塞编辑，红条提示第一行错）（Must · P0）
F-14 · Round-trip 保源码（引号/缩进/空行/trailing `\n` byte-for-byte）（Must · P0）
F-15 · TOML `+++...+++` 支持（Should · P1）
F-16 · JSON `{...}` frontmatter 支持（Could · P2 · 可推迟）

依赖：math-view 交互模式（T-3.9 已经 land · reference NodeView pattern）· 模块 a 的 roundtrip-tracker

需要新增 npm 依赖：
- `remark-frontmatter@^5`（可能已有 gfm 附带）
- `js-yaml@^4`
均在 `.tmp/milkdown-prod-builder/package.json` 添加

参考 pattern
- code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/math-view.template.js（NodeView 展开/折叠 + textarea + keymap 全套）
- code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/footnote.template.js（T-3.5c.2 · 最近的 NodeView + Round-trip 参照物）
- code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/roundtrip-tracker.template.js（blockId 通道）

━━━━━━━━━━━━━━━━━━━━━━
DoD
━━━━━━━━━━━━━━━━━━━━━━
1. 新增 frontmatter-view.template.js（$nodeSchema('frontmatter') 或复用 remark-frontmatter 输出的 yaml/toml node · NodeView folded/expanded 双态 · textarea 编辑 · js-yaml 校验）
2. helpers 模块 frontmatter-helpers.template.js（YAML/TOML parse safe wrap · summarize（title + 字段数）· 语法错误 first-line 定位）
3. `.tmp/milkdown-prod-builder/package.json` 新增 remark-frontmatter + js-yaml
4. build-milkdown-editor.cjs 补上述模板文件 + 依赖 mjs 拷贝
5. entry.template.js / verify.template.mjs 注册 frontmatter 插件 + Round-trip fixture（8 类：YAML 基本 / YAML 引号 / YAML 缩进 / YAML 空值 / YAML 错语法 / TOML / JSON / trailing-newline）
6. 新增单测 frontmatter.test.ts（helpers + NodeView state machine，≥10 case）
7. tsc 全项目 0 error（NODE_OPTIONS=--max-old-space-size=8192）
8. vsword 已有单测继续绿（基线 T-3.5c.2 之后 710/712）· mindmap 那 2 fail 与本任务无关继续容忍
9. Gate D roundtrip verify 全绿（含新增 fixture · parserRoundTripBytes 匹配）· Gate E/F 不回归
10. commit 本地不 push · title 'feat(vsword/milkdown): T-3.5c.3 frontmatter YAML+TOML+折叠+编辑+保源码'
11. kanban comment 报 commit hash + diffstat + bundle 增量 + 测试摘要 + 实施选择（含 JSON F-16 是否降级），然后 blocked({kind:'needs_input', reason:'review-required: T-3.5c.3 完成'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救（重要 · 本任务 PRD 估 40-50 迭代 · 60 上限风险大）
━━━━━━━━━━━━━━━━━━━━━━
- iteration 60 · 估 40-50
- 临近 40 优先 commit 已完成部分
- 优先级：**F-10/F-11/F-12/F-13/F-14（YAML 全流程）+ Round-trip + ≥6 case 测试 > F-15 TOML > F-16 JSON**
- JSON（F-16 Could）可以 wip 或降级：如果 remark-frontmatter 不原生支持 JSON，comment 说明"F-16 未做，转 T-3.5c.6 兜底"
- TOML（F-15 Should）若 remark-frontmatter 需要额外插件（remark-frontmatter-toml），先加尝试；撞壁则 comment 说明降级
- 实施细节自决，无方向性问题不问

指导：好看放一放 · 中文 · 未动块保源码优先 · 无方向性问题自决
