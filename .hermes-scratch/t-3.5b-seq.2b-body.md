# T-3.5b-seq.2b · js-sequence-diagrams NodeView Gate 验收

## 背景
T-3.5b-seq.2（`t_b9d24b81`）实施主体已完成：
- `webview/sequence-view.template.js` (~330 行) + `sequence-view-helpers.template.js` — NodeView + textarea 编辑
- `test/fixtures/roundtrip/typora/sequence-{basic,actors,notes}.md` — 3 P0 fixture
- `webview/entry.template.js` + `code-block-chrome.template.js` — wiring
- `build-milkdown-editor.cjs` — 依赖 + esbuild alias（跳过 rokt33r UMD wrapper 里 dead-code `require("fs")` / `require("path")`）

**build 已成功**（coordinator 亲自跑）：
- webview bundle 4.6MB · gzip 1.3MB
- 内嵌 roundtrip verify: `roundTrip.ok=true`

**当前状态**：working tree 有改动待验证 + commit + push，尚未 archive。

## 任务范围（只做验收，不改代码）
1. **Gate E** — 跑完整 275+ 项 round-trip（覆盖新增 sequence 3 fixture）
2. **Gate F** — mermaid + flowchart + sequence selfcheck（22 类基线 + flow + sequence 新类）
3. **Gate G** — 性能 p95 6 项门槛
4. **tsc** — 0 error

## 验收命令
参考 T-3.5b-flow.2（`t_797d5b78` commit `72562ecf`）的收官流程 —— 那次是同一套 Gate 命令。

## 通过条件（全绿才 unblock）
- Gate E: 275/275 及以上（新增 sequence 3 项加入基线）
- Gate F: 25/25（22 基线 + flow + sequence）或以上
- Gate G: 6/6
- tsc: 0 error

## 汇报格式（review 消息必须带类型标签）
- ✅ 全绿 → blocked with reason: `review-required: 全 Gate 绿 · sequence 3 fixture 已加入 Gate E 基线 · commit <hash>`
- ❓ 有 Gate 挂 → blocked with reason: `方案敲定: Gate X 挂 N 项 · 详情见 comment`（**不许自作主张修代码**，coordinator 拍板下一步）

## 预算
40 iter · 60min · 只跑 Gate 不写代码，应绰绰有余

## 后续
- 全绿 → coordinator commit + push origin dev
- 有挂 → coordinator 决定修/回滚
