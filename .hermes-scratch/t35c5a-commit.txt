feat(vsword/milkdown): T-3.5c.5a code-block info meta 保真

范围收窄（setext 拆到 T-3.5c.5b 单独派）：本 commit 只落地 F-21 code-block
fence info meta round-trip 保真，配套 fixture + verify 断言。

改动清单
─────────────────────────────────────
- webview/code-block-schema-override.template.js（新增 93 行）
  $nodeSchema('code_block') 覆盖：attrs 追加 meta:string（默认 ''），
  parseMarkdown 从 mdast node.meta 读入，toMarkdown 写回 node.meta；
  toDOM 附加 data-meta 便于 e2e 调试，不影响 Prism 高亮。
- webview/entry.template.js（+4）在 .use(commonmark) 后 .use(codeBlockSchemaOverride)
  覆盖上游 codeBlockSchema。同 id 后 use 覆盖前 use（与 image-schema-override 同模式）。
- build-milkdown-editor.cjs（+3）挂载 .template.js → .mjs 拷贝。
- webview/verify.template.mjs（+14）source 追加 js/ts 带 meta 的两段 fence，
  checks 追加 6 条断言：js/ts 头行完整保真、mermaid/python 白名单不被 meta 分支
  误伤、schema override 导出存在。

Fixture
─────────────────────────────────────
- test/fixtures/roundtrip/typora/code-fence-meta.md（新增）
  5 段：js meta / ts 双 meta（含空格 + 引号）/ python 无 meta / mermaid 无 meta / 无 lang。
- test/node/milkdownRoundtrip.test.ts sanity 30 → 31（用于 F-21 验证）。

Mermaid 白名单
─────────────────────────────────────
attrs.meta 与其他 code_block 共用同一 pipeline：mermaid 源码本身无 fence meta，
即便有也按同一 pipeline 忠实回写 → 白名单靠"上游就没有 meta"这条自然事实兜底，
无需 lang === 'mermaid' 特判。Gate F 70/70 全绿证明 mermaid NodeView 未被误伤。

DoD 验证
─────────────────────────────────────
- Gate E 一键：257/257 pass · 0 fail · 526 ms（含新 code-fence-meta.md）
- Gate F 一键：70/70 pass · 0 fail · 7.7 s（mermaid 白名单未回归）
- tsc 全项目 0 error（NODE_OPTIONS=--max-old-space-size=8192）
- roundtrip verify：outputBytes === parserRoundTripBytes === 1100，output
  中 js {highlight-lines=[1,3]} 与 ts {title="demo.ts" line-numbers}
  两段 fence 头行 byte-for-byte 保真
- 459 case protocol/session/serializer 单测集全绿

setext 保真拆到 T-3.5c.5b：coordinator comment 明确本轮只做 code meta。
Refs: PRD F-21 · AC-10 · §8 T-3.5c.5
