本子任务范围（T-3.5b-flow.2 · Flowchart.js NodeView + 懒加载 + P0 fixture）
━━━━━━━━━━━━━━━━━━━━━━

依据：
- PRD `docs/plans/003b-flowchart-jsseq.md` v1.1（全默认 Q1..Q5=a 锁定）§8 T-3.5b-flow.2
- Spike 结论 `docs/spikes/T-3.5b-flow.1-report.md`（GO 无条件放行 · commit `1c92057b`）
  - flowchart.js@1.18.0 + raphael@2.3.0 · CSP 三大 API 全零命中 · 无需 patch
  - raphael 版本冲突 `overrides` 到 2.3.0 · P0 语法全绿
  - VML 分支运行时死代码可直接放行

━━━━━━━━━━━━━━━━━━━━━━
交付目标（DoD）
━━━━━━━━━━━━━━━━━━━━━━

**mirror mermaid-view.template.js pattern**（模块 b 已产 · 直接复制骨架改内容）

1. **NodeView 模板**：`code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/flowchart-view.template.js`（≤ 400 行）
   - 挂 `code_block[lang=flow]` 通道（PRD §5 F-01 · Typora fence）
   - Inline WYSIWYG：进节点显示 SVG · 点击进入 textarea 编辑 · live preview
   - Esc 取消 / Ctrl+Enter 或失焦 commit（走 `tr.setNodeMarkup` 保持 code_block）
   - **必须复用**（`math-view.template.js` T-3.9 三条 pitfall）：
     - `stopEvent` + `ignoreMutation`（atom + interactive children pattern）
     - 失焦 vs Esc 语义分离
     - `paper.remove()` + `container.innerHTML=''` 清理防泄漏（R-5）
   - textarea input **debounce 200ms**（R-6）

2. **Helpers 模块**：`flowchart-view-helpers.mjs`
   - `parseFlowchart(source)` · `renderToSvg(diagram, container)` · error banner 提取
   - 纯函数策略模块（模仿 frontmatter/emoji helpers）

3. **懒加载 chunk 配置**：
   - `build-milkdown-editor.cjs` 新增 entry：`flowchart.chunk.js` + `raphael.chunk.js`（共享）
   - webview 首屏 stub：`window.__vswordFlowchartLoader()` 动态 `import('./flowchart.chunk.js')`
   - 首个 `code_block[lang=flow]` 进入视口/挂载时才 load
   - **实证首屏 bundle 未增加**（对比前后 `webviewBundleGzipBytes`，允许 ≤ 5KB stub 增量）

4. **依赖装到主 package.json**：
   - `code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/package.json` 或工作区 root（哪里放 mermaid 就哪里放）
   - `flowchart.js@1.18.0` + `raphael@2.3.0`
   - **root package.json 加 `overrides`** 强制 raphael 统一到 2.3.0（seq.2 也会消费）

5. **P0 fixture（PRD §5.2.1）**：
   - 新建 `code-oss/src/vs/workbench/contrib/vsword/test/fixtures/roundtrip/typora/flowchart-*.md`（P0 六节点 + 决策分支边）
   - `code_block[lang=flow]` fence · Round-trip byte-for-byte
   - Gate E 通道跑通（roundtrip serialize 不破源码）

6. **verify.template.mjs 断言**（Gate E/F）：
   - `flowchartRoundTripKeepsSource` · `flowchartInlineRenderProducesSvg` · `flowchartClickToEditWorks` · `flowchartEscCancels` · `flowchartEnterCommits` · `flowchartErrorBannerShows`

7. **通过验收标准**（PRD §6）：
   - AC-1 · AC-3 · AC-4 · AC-5 · AC-6 · AC-9
   - AC-7（懒加载） 与 AC-8（P0 fixture 全绿）在本任务落地部分能力，收官交给 T-3.5b-flowseq.3
   - AC-10（导出 svg/png）不做（后置）

━━━━━━━━━━━━━━━━━━━━━━
硬约束
━━━━━━━━━━━━━━━━━━━━━━

- **Round-trip 零破坏**：mermaid 已验证的 code_block 通道 pattern 直接复用 · `blockIdAllocator` / `roundtripSerializer` **不改动**
- **主题联动**：本任务先写死 light 主题跟随（AC-6 主线） · dark 主题映射预留常量点 · 完整主题联动交给 T-3.5b-flowseq.3
- **好看放一放**：只求功能过 gate，CSS 精调延后
- **tsc 0 error**：手写 minimal `.d.ts`（C-9）给 flowchart.js API（`parse` / `Diagram.drawSVG`）
- **不 push**：模块 b 剩余部分累积到 flowseq.3 收官统一 push
- 完成后 blocked({kind:'needs_input', reason:'review-required: T-3.5b-flow.2 完成 · commit <hash>'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━

- iteration 60 · 估 30-45（NodeView + 懒加载 + fixture + verify + tsc）
- 临近 45 优先 commit 已完成部分 · 允许 wip 降级
- 优先级：**NodeView + Gate E fixture > 懒加载 > verify 断言 > 完整主题**
- 无方向性问题自决

指导：好看放一放 · 中文 · 复制 mermaid-view.template.js 骨架 · 独立于 T-3.5b-seq.2 并行
