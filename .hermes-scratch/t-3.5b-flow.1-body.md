本子任务范围（T-3.5b-flow.1 · Flowchart.js + raphael CSP 可行性 spike）
━━━━━━━━━━━━━━━━━━━━━━

依据 PRD docs/plans/003b-flowchart-jsseq.md（v1.1 锁定 · 用户全默认 Q1..Q5=a）§8 T-3.5b-flow.1、§7 C-2、§9 R-1/R-2/R-3。

Spike 目标（结论备忘导向，非产品级实现）
━━━━━━━━━━━━━━━━━━━━━━

1. **CSP 静态扫描**：
   - 装 `flowchart.js@1.18.0` + `raphael@2.3.0`（用 pnpm/npm 到 sandbox，如 `.tmp/flow-spike/`）
   - 扫 chunk 内是否含 `new Function`、`eval(`、`document.write`
   - 命令示例：`rg -n 'new Function|eval\(|document\.write' node_modules/flowchart.js/release/ node_modules/raphael/`
   - 记录每条命中的文件位置 + 上下文；判定是否触发 webview CSP（`default-src 'self'` + `script-src 'self'`）
   - **R-2 重点**：raphael VML 分支（IE 兼容遗留），能否用 esbuild define `RAPHAEL_HAS_VML: false` 剔除

2. **最小 fixture 渲染 demo**（跑得通即可，不接 Milkdown NodeView）：
   - 建 `.tmp/flow-spike/demo.html`：ESM 引入 flowchart.js + raphael，喂一段 P0 语法（`start`/`op`/`cond`/`->`/`(yes)->`/`(no)->`）
   - 用 `node -e` + jsdom 或直接浏览器 file:// 打开验证渲染
   - 截屏 or SVG dump 说明"绘出来了"

3. **bundle 尺寸实测**：
   - esbuild `--bundle --minify` 打 flowchart.js 单独入口 → 记录 min+gzip 尺寸
   - esbuild 单独打 raphael → 记录 min+gzip 尺寸
   - **验证 PRD 预估**：flowchart chunk ≈ 110-130 KB + raphael 走共享 chunk

4. **raphael 版本冲突验证**（R-3）：
   - flowchart 声明 raphael@2.3.0，sequence 声明 raphael@~2.1.x
   - 本 spike 只用 raphael@2.3.0，验证 flowchart.js 在 2.3.0 下 P0 语法全绿
   - 记录 `overrides` 到 root package.json 的方案样本（预留给 seq.1 消费）

5. **raphael chunk 拆分方案设计**（不实施，只出结论）：
   - 记 esbuild 里如何用 external 或 splitChunk 让 flowchart + sequence 共享一个 raphael chunk
   - 参考 mermaid chunk 拆法（`.tmp/milkdown-prod-builder/` 里已有 pattern）

━━━━━━━━━━━━━━━━━━━━━━
DoD（spike 交付物）
━━━━━━━━━━━━━━━━━━━━━━

1. 建 `docs/spikes/T-3.5b-flow.1-report.md`（不进 code，只进 docs），含：
   - CSP 扫描原始输出（rg 结果贴齐）+ 判定结论（通过 / 需 patch / 阻塞）
   - fixture 渲染证据（SVG 片段 or 截图路径）
   - bundle 尺寸实测数据（flowchart / raphael 分别 min+gzip）
   - raphael 版本冲突方案样本（package.json overrides 片段）
   - raphael chunk 拆分方案（伪代码 / esbuild 配置片段）
   - **明确结论**：go / no-go / conditional-go（说明前置条件）

2. commit `docs(spikes): T-3.5b-flow.1 Flowchart.js CSP+bundle spike report`
   - 本地不 push
   - 只提交 `docs/spikes/T-3.5b-flow.1-report.md`（及必要的最小 demo html 若不大于 5 KB）
   - `.tmp/flow-spike/` 目录**不提交**（加 .gitignore 或 sandbox 用一次即删）

3. kanban comment 报 commit hash + report 关键结论一段话（≤500 char），然后 blocked({kind:'needs_input', reason:'review-required: T-3.5b-flow.1 spike 完成 · go/no-go 见 report'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━

- iteration 60 · spike 估 20-30 迭代（不写产品代码）
- 只装到 sandbox 目录，不动 vsword 主构建
- 若 npm install 失败（网络/包不存在）→ 立即在 comment 里说明 + blocked（不硬耗迭代）
- 若 CSP 扫描发现严重阻塞 → 也照写 report + comment 结论（no-go 也是有效交付）
- 无方向性问题自决 · P0 语法列表见 PRD §5.2.1

指导：好看放一放 · 中文 · spike 结论优先 · 无方向性问题自决 · 与 T-3.5b-seq.1 并行独立
