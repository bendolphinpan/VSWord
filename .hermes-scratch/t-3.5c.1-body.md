本子任务范围（T-3.5c.1 · Emoji shortcode）
━━━━━━━━━━━━━━━━━━━━━━

依据 PRD docs/plans/003c-syntax-completion.md（F-01/02/03/04 · AC-1/2/3/4 · §8 T-3.5c.1）：

F-01 · shortcode → 图形渲染
- 输入 `:smile:` → 显示 🙂（Unicode）
- 未识别 shortcode（如 `:notarealone:`）不 mark 化，保持文本原样

F-02 · inputRule
- 触发字符：空格，形如 `:name: ` → 转 emoji mark
- Esc 撤销为 shortcode（可选，若实现复杂可跳过在 comment 说明）

F-03 · 保源码
- **默认策略：保 shortcode**（不改成 unicode）
- 序列化回 md 保持 `:name:` 原文

F-04 · 未识别兜底
- `:notarealemoji:` 保文本，不进 emoji mark，不报错

依赖包（PRD §7 NG7 / §14 R4）
- `remark-emoji@^4`
- `node-emoji@^2`
- 进 `.tmp/milkdown-prod-builder/package.json`
- Milkdown vendor **不动版本**（冻结 @milkdown/*@7.21.2）
- bundle 增量控制在 +50KB（可推翻，若超也可 commit 后 comment 说明）

参考现有 pattern
- $markSchema + $remark + $inputRule 四件套（highlight/underline/sub-sup 已跑通）
- code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/highlight.template.js
- code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/sub-sup.template.js（T-3.5c.4 · 最近的参照物，双分支 helper 模式）

Round-trip 契约
- 走已有 roundtrip-tracker · 未编辑块保源码
- emoji mark 是 inline mark，不影响 blockIdAllocator

━━━━━━━━━━━━━━━━━━━━━━
DoD
━━━━━━━━━━━━━━━━━━━━━━
1. 新增 emoji.template.js（$markSchema + $remark + $inputRule + 双向 handler）
2. 新增 emoji-helpers.template.js（若需 helper 模块，参照 sub-sup-helpers 模式）
3. `.tmp/milkdown-prod-builder/package.json` 加 remark-emoji + node-emoji
4. build-milkdown-editor.cjs 新增 emoji.mjs / emoji-helpers.mjs 拷贝
5. entry.template.js / verify.template.mjs 注册 emoji 插件
6. 新增单测 emojiShortcode.test.ts（≥6 case: 识别 · 序列化保 shortcode · Esc 撤销 · 未识别兜底 · CJK 邻近 · Round-trip）
7. tsc 全项目 0 error（NODE_OPTIONS=--max-old-space-size=8192）
8. vsword 已有单测继续绿（基线 T-3.5c.5b 之后 · Gate E 264 · Gate F 70）
9. Gate D roundtrip verify 继续绿
10. commit 本地不 push · title 'feat(vsword/milkdown): T-3.5c.1 emoji shortcode 保源码'
11. kanban comment 报 commit hash + diffstat + bundle 增量 + 测试摘要，然后 blocked({kind:'needs_input', reason:'review-required: T-3.5c.1 完成'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救（重要 · 已连撞 3 次 gave_up）
━━━━━━━━━━━━━━━━━━━━━━
- iteration 60 · 估 30-40 迭代
- 临近 40 优先 commit 已完成部分
- 优先级：**核心 markSchema + inputRule + 序列化 + 至少 3 case 测试 > 全测试 > Esc 撤销 > bundle 优化**
- 如果 remark-emoji 生态与现有 gfm preset 冲突（strikethrough `~~` / footnote `[^1]`），选自研 remark plugin 手写映射表（用 node-emoji.get(name)），comment 说明
- 无方向性问题自决，实现细节自己拍板

指导：好看放一放 · 中文 · 未动块保源码优先 · 无方向性问题自决
