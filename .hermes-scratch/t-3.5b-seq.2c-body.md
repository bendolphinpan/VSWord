# T-3.5b-seq.2c · Gate E + G 挂点小面修复 + 重跑

## 背景
T-3.5b-seq.2b（`t_b4d8c98b`）连撞预算 2 次未归档。coordinator 已亲自跑一次 build（从 `code-oss/` 目录）：
- **build 已绿**：roundTrip.ok=true, bundle 4.6MB, gzip 1.3MB
- **Gate F 70/70 全绿**（mermaid + sequence 相关）
- **Gate E 289/290**：唯一挂点 = sanity 断言硬编码 fixture 数
- **Gate G 挂**：因为 gate-g.mjs 从 repo root 调 build，打破 build.cjs 的 cwd 假设

## 任务范围（极小 · 只改 2 个数字/1 行 · 无新增代码）
1. **修 Gate E sanity 计数**
   - 文件：`code-oss/src/vs/workbench/contrib/vsword/test/node/milkdownRoundtrip.test.ts` line 821-826
   - 把 `37` → `40`（含 3 个新 sequence fixture: sequence-basic/actors/notes）
   - 注释里 "34 → 37" 也要顺手改成 "37 → 40" 并追一句 "T-3.5b-seq.2 新增 3 项 sequence fixture"

2. **修 Gate G build cwd**
   - 文件：`code-oss/test/scripts/gate-g.mjs` 约 line 129-135（`runStep` 函数里的 `spawnSync`）
   - 现状：`cwd: REPO_ROOT` 对所有子步生效
   - 修法（选一，倾向 A）：
     - A. 在 STEPS 里给 build 步加 `cwd: CODE_OSS`，runStep 里 `cwd: step.cwd || REPO_ROOT`
     - B. 或直接让 build 步的 `spawnSync` 硬编码 `cwd: CODE_OSS`
   - 原因：`build-milkdown-editor.cjs` 内部 `codeOssRoot = process.cwd()`，从 REPO_ROOT 跑会让 workspaceRoot 计算错位，esbuild alias `empty-shim.js` 找不到

3. **重跑 3 个 Gate 确认全绿**
   ```bash
   cd /d/GIT/VSWord
   node code-oss/test/scripts/gate-e.mjs         # 期望 290/290
   node code-oss/test/scripts/gate-f.mjs         # 期望 70/70
   node code-oss/test/scripts/gate-g.mjs         # 期望 6/6
   cd code-oss && node node_modules/typescript/bin/tsc --noEmit -p src/tsconfig.json  # 期望 0 error
   ```

## 通过条件（全绿才 unblock）
- Gate E: 290/290
- Gate F: 70/70
- Gate G: 6/6（build + gate-e + gate-f + perf + roundtrip-selfcheck + mermaid-selfcheck 全 ✅）
- tsc: 0 error

## 汇报格式（review 消息必须带类型标签）
- ✅ 全绿 → block reason: `review-required: 全 Gate 绿 · Gate E 290/290 · Gate F 70/70 · Gate G 6/6 · commit <hash>`
- ❓ 还有 Gate 挂 → block reason: `方案敲定: Gate X 挂 · 详情见 comment`（不许扩大改动面）

## 预算自救（关键）
- **20 iter · 30min · 只改 2 处 + 跑 3 个 Gate**
- 如果 15 iter 内还没跑到 Gate G → **立即** block review-required 报进度，不要贪修
- 不允许扩大改动面（不重构 gate-g、不动 build.cjs alias 逻辑、不动 sequence-view）
- Gate 报告落 `code-oss/test/reports/gate-{e,f,g}-*.md`，coordinator 会看

## 上下文
- 前置：seq.2b 撞预算 2 次（`t_b4d8c98b` run 69 timed_out + run 70 gave_up）
- 上游 commit: `72562ecf` (T-3.5b-flow.2)
- HEAD working tree 已有 seq.2 主体产出（3 fixture + 2 template + build.cjs alias + entry wiring），**保留不动**
- 参考文档：`~/AppData/Local/hermes/skills/project-management/project-coordinator/references/esbuild-umd-ded-code-require.md`（诊断历史）

## 后续（coordinator 干）
- 全绿 → coordinator commit 一次性收 seq.2 全部产出 + push origin/dev
- 派 T-3.5b-seq.3（模块 b 收官汇总卡 or 直接进 T-3.7b 源码/预览切换 PM 卡）
