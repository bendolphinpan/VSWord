本子任务范围（T-3.5b-seq.2 · js-sequence-diagrams NodeView + 懒加载 + P0 fixture）
━━━━━━━━━━━━━━━━━━━━━━

依据：
- PRD `docs/plans/003b-flowchart-jsseq.md` v1.1（全默认锁定）§8 T-3.5b-seq.2
- Spike 结论 `docs/spikes/T-3.5b-seq.1-report.md`（GO 无条件放行 · commit `71c960ab`）
  - `@rokt33r/js-sequence-diagrams@2.0.6-2` + raphael@2.3.0 + underscore@1.4.4
  - **关键发现**：fork 实际用 jison 0.4.15 预编译（不是 PEG.js）· R-1 首要风险不存在
  - CSP 三大 API dist 产物全零 · Chromium `securitypolicyviolation` 0 次
  - P0 语法全渲染成功 · gzip 34.5 KB（远优于预估）
  - **Q2 兜底方案 vendor + 预编译 PEG 不再需要**

━━━━━━━━━━━━━━━━━━━━━━
交付目标（DoD）
━━━━━━━━━━━━━━━━━━━━━━

**mirror mermaid-view.template.js 与 flowchart-view.template.js（如已完成）pattern**

1. **NodeView 模板**：`code-oss/src/vs/workbench/contrib/vsword/browser/milkdownEditor/webview/sequence-view.template.js`（≤ 400 行）
   - 挂 `code_block[lang=sequence]` 通道（PRD §5 F-02 · Typora fence）
   - Inline WYSIWYG + click-to-edit + live preview + Esc/Ctrl+Enter commit
   - **复用 mermaid pitfall**：`stopEvent` + `ignoreMutation` · 失焦 vs Esc · `paper.remove()` 清理
   - **sequence 特殊**：`_.getFont/_.setSize/_.setFinish` etc. 通过 fork API 调用 · `Diagram.parse(source)` + `diagram.drawSVG(container, {theme:'simple'})`
   - 主题默认 `simple`（D-5 · 不加载 webfont · 快）· `hand` 主题 opt-in（后置）
   - textarea input debounce 200ms

2. **Helpers 模块**：`sequence-view-helpers.mjs`
   - `parseSequence(source)` · `renderToSvg(diagram, container, theme)` · error banner 提取

3. **懒加载 chunk 配置**：
   - `build-milkdown-editor.cjs` 新增 entry：`sequence.chunk.js`
   - **消费 T-3.5b-flow.2 产出的 `raphael.chunk.js`**（共享，D-3 · Q3=a）
   - underscore 也进 sequence.chunk（fork 静态依赖）
   - 首屏 stub · 首个 `code_block[lang=sequence]` 挂载时才 load

4. **依赖装到主 package.json**：
   - `@rokt33r/js-sequence-diagrams@2.0.6-2`
   - `underscore@1.4.4`
   - raphael 消费 flow.2 已加的 `overrides@2.3.0`

5. **P0 fixture（PRD §5.2.2）**：
   - `test/fixtures/roundtrip/typora/sequence-*.md`
   - P0 语法：`Actor A` / `A->B: msg` / `A-->B: msg` / `A->>B: msg` / `A-->>B: msg` / `Note left of A` / `Note right of A` / `Note over A,B` / `participant A as "别名"`
   - Round-trip byte-for-byte · Gate E 通过

6. **verify.template.mjs 断言**：
   - `sequenceRoundTripKeepsSource` · `sequenceInlineRenderProducesSvg` · `sequenceClickToEditWorks` · `sequenceEscCancels` · `sequenceEnterCommits` · `sequenceErrorBannerShows` · `sequenceThemeIsSimpleByDefault`

7. **通过验收标准**（PRD §6）：
   - AC-2 · AC-3 · AC-4 · AC-5 · AC-6 · AC-9
   - AC-7 · AC-8 与 flow.2 一同交给 T-3.5b-flowseq.3

━━━━━━━━━━━━━━━━━━━━━━
硬约束
━━━━━━━━━━━━━━━━━━━━━━

- **与 T-3.5b-flow.2 并行独立**：都改 `build-milkdown-editor.cjs` 时如果 flow.2 未完成先加占位 entry · **合并冲突留给 flowseq.3 收官处理**
- 若 flow.2 先落地 raphael chunk 结论 → 本任务直接消费；若未落地 → 本任务先自己引 raphael@2.3.0 · flowseq.3 收敛
- **手写 minimal `.d.ts`**（C-9）：`Diagram.parse` / `Diagram.prototype.drawSVG` / `Diagram.prototype.setTheme` 等 API
- **好看放一放** · **tsc 0** · **不 push**
- 完成后 blocked({kind:'needs_input', reason:'review-required: T-3.5b-seq.2 完成 · commit <hash>'})

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━

- iteration 60 · 估 30-45
- 临近 45 优先 commit · 允许 wip 降级
- 优先级：**NodeView + Gate E fixture > 懒加载 > verify > 主题**
- 无方向性问题自决

指导：好看放一放 · 中文 · 复制 mermaid/flow NodeView 骨架 · 独立于 T-3.5b-flow.2 并行 · Q2 兜底不必再考虑
