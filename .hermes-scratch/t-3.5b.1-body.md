本子任务范围（T-3.5b.1 · Mermaid Spike）
━━━━━━━━━━━━━━━━━━━━━━

依据 PRD docs/plans/003b-mermaid.md §8 T-3.5b.1：
Spike：mermaid v11 在 Milkdown webview CSP 下的可行性验证

**默认假设已锁**（PRD §10 D-1..D-5 · 无方向分歧）：
- D-1 inline WYSIWYG（math-view 同款）
- D-2 完全懒加载（首屏 0 KB）
- D-3 主题跟随 Code OSS light/dark（写死映射，不做 config）
- D-4 语法错误 = 保留上次成功渲染 + 顶部错误条
- D-5 P0/P1/P2 按 §5.2 表（v11 GA / beta 分级）

━━━━━━━━━━━━━━━━━━━━━━
Spike 交付物（DoD）
━━━━━━━━━━━━━━━━━━━━━━

1. **可行性备忘 `docs/spikes/T-3.5b.1-mermaid-feasibility.md`**（新建）
   - Mermaid v11.x 最新稳定版本号（查 npm registry）
   - Milkdown webview 现有 CSP（读 `entry.template.js` / `milkdown-editor.html` 之类的 CSP 头）
   - Mermaid 是否使用 `eval` / `new Function` / dynamic import script（静态扫描 mermaid ESM bundle）
   - 结论：能否直接 mount / 需要 CSP relax / 需要 patch mermaid
2. **最小 fixture 渲染 demo**
   - 挂一个最小 fixture：`test/fixtures/mermaid/spike-flowchart.md`（内含 ` ```mermaid\nflowchart LR\n  A --> B\n``` `）
   - Node 端跑 mermaid.render() 生成 svg 字符串，输出到 `test/reports/mermaid-spike-<ts>.svg`
   - 打印 svg 字节数
3. **Bundle 尺寸实测**
   - `npm view mermaid@11 dist` 或直接下一次 `mermaid.esm.min.mjs` 到临时目录
   - min+gzip 实际字节
   - 结论：满足 F-06 懒加载硬约束（首屏 0 KB · 图存在时才 dynamic import）
4. **接入路径预判**
   - 参考 `math-view.template.js` NodeView pattern，列出 T-3.5b.2 需新增/改动的文件清单
   - 参考 `code-block-chrome.template.js`（mermaid 走 code_block[lang=mermaid] 通道）
   - 参考 `roundtrip-tracker.template.js`（mermaid fence 走 code_block blockId 通道，Round-trip 契约无需破）

━━━━━━━━━━━━━━━━━━━━━━
硬约束
━━━━━━━━━━━━━━━━━━━━━━

- **只 spike，不实施**：不要开始写 mermaid-view NodeView（那是 T-3.5b.2）
- **只出结论 + demo**：允许 npm install 试验（但用 spike 目录，别污染主 package.json）
- **不改 code-oss/src/** 主代码（除非要读 CSP 头）
- **不 commit 主 package.json 变更**（可以在 spike md 记录"需要新增依赖 mermaid@11.x.y"）
- **commit** spike md + fixture + spike report svg 到 `docs/spikes/` `test/fixtures/mermaid/spike-*` `test/reports/mermaid-spike-*.svg` · title `spike(vsword/milkdown): T-3.5b.1 mermaid v11 CSP 可行性验证`
- **本地不 push**（模块 b 首个子任务，收官统一 push）

━━━━━━━━━━━━━━━━━━━━━━
预算自救
━━━━━━━━━━━━━━━━━━━━━━

- iteration 60 · 估 15-25
- 临近 45 优先 commit 已完成部分
- 优先级：**CSP 静态扫描 + bundle 尺寸 + 结论备忘 > 最小 demo svg > 接入路径预判**
- 前 3 项必产出，后 1 项可推迟到 kanban comment
- 无方向性问题自决

指导：好看放一放 · 中文 · 只 spike 不实施 · 完成后 blocked({kind:'needs_input', reason:'review-required: T-3.5b.1 spike 完成'}) 让 coordinator review 结论
